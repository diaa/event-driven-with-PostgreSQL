apiVersion: 1

providers:
  - name: "postgres-cdc"
    orgId: 1
    folder: "Postgres CDC"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
